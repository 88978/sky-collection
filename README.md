# Sky Collection

A set of three symbolic on-chain art contracts:

- **SunArt.sol** → ☀️
- **WindArt.sol** → 💨
- **StormArt.sol** → 🌩️⚡

Each contract exposes a single string variable `art`.
